# Q12 relay — 2026-05-04 (post-(c.3) audits)

## Context

(c.3) part 1 (κ vs radius) and part 2 (sibling-edge-to-edge) audits
committed at `052d809` and `035b1d4`. Both are audits per the
pre-code commitment; no recognisability or substitution machinery
touched. Three sub-questions sent to Claude (web).

## Reply from Claude (web)

### Q12a — Authorise. Implement exact-ZPhi polyhedron intersection.

The sibling-face-to-face check is load-bearing for Track A's
Goodman-Strauss citation. Two days well-scoped; infrastructure
reusable for (c.1)'s k_max lift (current `realise()` uses
AABB + SAT approximations; exact intersection would strengthen it)
and any cut-and-project work.

Implementation note: convex polyhedron intersection over ZPhi
reduces to halfspace-intersection. Each face defines a halfspace
{x : n·x ≤ d} with n a ZPhi vector and d a ZPhi scalar. For
tetrahedra (4 faces each, 8 halfspace constraints total),
explicit halfspace enumeration is ~50 lines, fully exact, no
general GJK needed.

On failure: document the specific child-pair offenders in
`audit_sibling_edge_to_edge.md`, note that matching-rule
construction requires edge-set refinement before Goodman-Strauss
§1.4 applies. Do not modify the Paolini dissection — the failure
is an audit finding, not a bug.

### Q12b — Authorise all three κ follow-ups, in order (i) → (iii) → (ii).

Reorder: docstring first (no risk), then verify patch construction
(30 min, no new math), then compute κ_ABCK (1 hour, only new
computation).

Reason: if (iii) reveals the pillar-2 patch was built with
n < κ_ABCK, that's a retraction of a Track A result and changes
what (ii) means — know it before computing κ.

**Docstring contract (verbatim from Claude (web)):**

> `is_recognisable(patch, rule, radius=r)` returning `True` is a
> valid pillar-2 witness iff (a) `patch` was constructed as
> `expand_supertile(rule, tile, level=n)` for some n, and
> (b) `n ≥ κ_rule` where κ_rule is the recognisability depth of
> `rule` in the Goodman-Strauss sense. Condition (b) is the
> caller's responsibility; this function does not verify it.

### Q12c — Serialise. The slight preference is correct; strengthen it.

Output 2's (b.2) possibility (pipeline-gap artefact) is not yet
ruled out. κ gap and sibling-face-to-face gap are both potential
(b.2) artefacts. If either follow-up reveals a pipeline problem,
(c.1) sweep results would need reinterpretation — not retraction,
but reframing.

Practical cost of serialising is low (~2.5 days total: 2 days
intersection + 2 hours κ). Cost of parallelising and then
discovering a pipeline gap is reframing a week's compute results.

**Concrete gate for (c.1) start:** both audit documents must have
a "finding: pipeline sound / pipeline gap identified" conclusion
before any (c.1) code lands. Audits already exist; they need a
conclusions section, not more investigation.

## Authorised work order

1. Q12b (i): update `is_recognisable` docstring with the
   bridging-lemma contract (verbatim from above).
2. Q12b (iii-a): find Track A pillar-2 test, extract the σⁿ depth
   used.
3. Q12b (ii): compute κ_ABCK from the Paolini dissection.
4. Q12b (iii-b): compare n vs κ_ABCK; flag if n < κ_ABCK.
5. Q12a: implement exact ZPhi tetrahedron intersection, run
   sibling-face-to-face audit on σ(A), σ(B), σ(C), σ(K).
6. Conclusion sections in `audit_kappa_vs_radius.md` and
   `audit_sibling_edge_to_edge.md` ((c.1) gate).
