# Q14 relay — 2026-05-04 (post-(c.1) closure, ready for cut-and-project read)

## Context

(c.1) closure: 78 candidates exhaustively NoRealisation across the
entire 9-class H₃ slice (max_entry ≤ 3, k_max=7). 144
SKIPPED_TAXONOMY_GAP. Bonus finding from (c.1.1): ABCK tilings ARE
model sets from D₆ via CPS (Frettlöh §1.3, Theorem 1.3) — direct
paper-cited support for Q11c's cut-and-project pivot.

## Reply from Claude (web)

### Q14a — Confirming. 65 exhaustive NoRealisation are load-bearing.

The 65 candidates that ran constitute a complete computational
survey of the 9-class H₃ slice at max_entry ≤ 3 — every volume-
compatible shape triple was attempted, every one returned
NoRealisation. The 144 unattempted candidates say the 15-class
extension would enlarge the pool, but say nothing about
realisability of the enlarged set. Distinct empirical questions.

**Required precise scoping** for `literature_review.md`:

> "No icosahedral substitution monotile with PF eigenvalue φ³
> exists among n ≤ 3 prototile rules with integer entries ≤ 3 and
> prototile shapes drawn from the 9 H₃-compatible tetrahedral
> classes derivable from the Paolini ABCK vertex pool. This is the
> first systematic computational survey of this family."

A precisely-scoped negative result is publishable; an overclaimed
one invites obvious objections.

### Q14b — Authorise Kramer-Neri 1984 + Kramer et al. 1994 read.

Same shape as the Goodman-Strauss read. The (c.1.1) Theorem 1.3
finding makes these reads directly productive rather than
speculative.

**Synthesis questions (the original three plus two additions):**

(a) What does the cut-and-project framework say about icosahedral
    monotile candidates specifically?
(b) What windows yield F-type (ABCK-family) tilings vs other
    families?
(c) Does window-deformation have any literature precedent for
    monotile candidate generation?
(d) **NEW:** Does the window-deformation literature treat window
    as a continuous parameter or a discrete choice? This determines
    whether "window search" is continuous optimisation or
    combinatorial enumeration — substantially affects infrastructure
    design.
(e) **NEW:** Relationship between the three vertex-class windows
    (Frettlöh §1.3, Fig 3) and the prototile shape? If the window
    determines the tile shape, search strategy flips: instead of
    enumerating substitution matrices and asking which realise,
    enumerate window shapes and ask which produce a single
    prototile type.

If (d) or (e) aren't answerable from Kramer-Neri and Kramer-
Papadopolos alone, note the gap and flag for a subsequent
Frettlöh-specific read.

### Q14c — Defer ABCK-Book sourcing.

Two reasons: (1) Frettlöh's "contains several errors" warning
means cross-validation is multi-day work, not a reference check.
(2) The cut-and-project read may reveal that the 15-class
extension is the wrong direction entirely (if the framework
suggests a non-H₃ shape family).

Keep the 144 taxonomy-gap candidates in cold storage with the
existing `SKIPPED_TAXONOMY_GAP` tag. Either the 15-class extension
or the cut-and-project read resolves their status; neither
requires action now.

## Authorised work order

1. Update `literature_review.md` with the precisely-scoped
   negative-result statement (verbatim from Q14a above).
2. Fetch Kramer-Neri 1984 and Kramer-Papadopolos-Schlottmann-
   Zeidler 1994 PDFs.
3. Read both papers; capture (paper, §, claim, relevance) notes
   in `literature_notes.md`.
4. Synthesise findings against (a)-(e) into a new section in
   `literature_review.md`. Re-relay before any infrastructure
   work lands.
