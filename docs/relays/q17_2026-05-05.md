# Q17 relay — 2026-05-05 (post-Hammock 2018, gate re-evaluation)

## Context

Two substantive reads since Q16 (Papadopolos-Hohneker-Kramer 1999
+ Hammock-Fang-Irwin 2018) substantially strengthened the
structural multi-prototile conjecture. Three sub-questions sent
to Claude (web): does Hammock shift the gate verdict? Authorise
Branch B4 now? Authorise the eigenvalue-embedding test?

## Reply from Claude (web)

### Q17a — Gate B-minus, not Gate B. Add the H₃ shape-rigidity argument explicitly.

The shift from Gate C to a softer Gate B-minus is real and
significant. Hammock's empire check is an independent computation
cross-validated against Kramer-Papadopolos-Zeidler 1991 covering
**all 36 vertex configurations / 880 tile types / 4230 sectors**
of the canonical T*(2F) projection. Absence of any single-
prototile empire is a complete enumeration result, not a sampling
result.

The remaining gap: Hammock's empire check is over the *canonical*
D₆ projection window (Voronoi domain). The Kramer-Papadopolos
1994 "all windows" result would settle whether other windows
inherit the same multi-prototile constraint.

**Action: write the H₃ shape-rigidity argument explicitly in
`strategy_pivot.md §7`:**

> Prototile shapes are determined by the H₃ root system (Papadopolos
> Q15a confirmation), not by the window. Varying the window changes
> which vertex configurations appear but not what the tiles look
> like. An empire is a set of *tiles*, and if the tiles are fixed
> by H₃ regardless of window, then no non-canonical window can
> introduce new tile types that weren't present in the canonical
> projection. This constrains the space of possible single-
> prototile empires across **all** D₆ windows, not just the
> canonical one.

Flag explicitly: "this is an argument, not a proof; the missing
piece is whether vertex-configuration *structure* (not tile shape)
varies enough across windows to produce a single-prototile
empire." Precise residual gap.

### Q17b — Authorise Branch B4 now, conditionally. Don't wait for ILL.

Evidence pile is publishable now as a precisely-scoped negative
result. ILL would strengthen but is not a prerequisite. Risk of
waiting: ILL takes months or never arrives, delaying compelling
work.

**Conditional: scope must be exactly what's been established.**
Title: *"Computational evidence against 3D icosahedral Einstein
tiles in the D₆ cut-and-project framework"* — NOT "no 3D
icosahedral Einstein exists." Difference: well-scoped
computational survey (publishable, citable, useful) vs theorem
(not yet established).

**Concrete Branch B4 deliverable structure:**

`docs/no_go_draft.md` with four sections:

- (i) face-merge algebraic no-go (Track A, closed-form);
- (ii) substitution-matrix computational survey (Track B, 78
  candidates);
- (iii) empire-exhaustion evidence (Hammock 2018, external);
- (iv) open gap (ABCK-specific window, ILL pending).

Sections (i)-(iii) complete today; (iv) placeholder.

Target venues: *Discrete and Computational Geometry* or
*Experimental Mathematics* — both publish well-scoped
computational negative results with explicit open-question
framing.

**Start `no_go_draft.md` before any further code lands.**
Engineering has done its job; writing is the critical path.

### Q17c — Authorise (i) only. Defer (ii).

**(i) — DO IT.** Encode T*(2F) 8×8 substitution matrix from
Papadopolos eq. (8). Verify eigenvalue embedding M_ABCK ⊂
M_T*(2F) algebraically. Concrete value: explains *why* the
Track A face-merge no-go is structural rather than coincidental
— ABCK's eigenvalue structure is a strict restriction of
T*(2F)'s. Reviewers at DCG will ask for this.

**Implementation:** `tests/integration/test_t2f_embedding.py` —
single test:
1. Encode M_T*(2F) from Papadopolos eq. (8).
2. Compute characteristic polynomial.
3. Verify roots match Papadopolos eq. (13).
4. Check M_ABCK eigenvalues are a subset of M_T*(2F)'s spectrum.

Commit message: `test: algebraic embedding M_ABCK ⊂ M_T*(2F)
verified (Papadopolos eq. 8, 13)`. Cite test in
`no_go_draft.md §(i)`.

**(ii) — DEFER.** Reproducing Hammock's empire calculations in
ZPhi³ duplicates already-cross-validated work. Bottleneck is the
draft write-up, not independent re-computation. Revisit only if
a reviewer specifically challenges Hammock's methodology.

## Authorised work order

1. Add H₃ shape-rigidity argument to `strategy_pivot.md §7`
   (Q17a).
2. Q17c(i): implement `tests/integration/test_t2f_embedding.py`.
3. Q17b: write `docs/no_go_draft.md` §(i)-(iv).
4. Relay-back with draft for review **before** any further
   engineering lands.
