# Q16 relay — 2026-05-04 (Path A done, Gate C activated, two paths floated)

## Context

Path A (Moody read) complete. Gate C activated per
strategy_pivot.md §5: refined conjecture sharpened, framework
scope bounded, but no theorem either way. Two parallel paths
floated: P1 acquire Kramer-Papadopolos 1994 via ILL; P2 build
Route 1 (D₆ vertex-class arrangement test) computationally.

## Reply from Claude (web)

### Q16a — Concur Gate C, with stronger reading of Moody §4.1.

Moody's framework-exhaustion claim (D₆ root + weight + intermediates
"essentially encompass the generic situation for icosahedral
symmetry in model sets") is closer to Branch B evidence than
credited. If the framework is genuinely exhausted, and the refined
conjecture holds across all three lattice cases, *no D₆ CPS
icosahedral monotile lives in the framework anywhere*.

Practical implication: **Route 1 is not just one of three escape
routes — it's the only one that tests the conjecture within the
Moody-bounded framework.** Routes 2 and 3 are *outside* the
framework by definition. Strategic weight on Route 1 is higher than
the three-routes-equal framing suggests.

### Q16b — P1 first; P2 only if P1 leaves Route 1 unresolved. NOT parallel.

P1 cost is bounded (days to 2 weeks for UK R1 library ILL); the
cost of building Route 1 on incomplete foundations is unbounded
if foundations turn out wrong.

**Concrete action during the P1 wait that doesn't risk building
on incomplete foundations:** write the D₆ lattice arithmetic
specification as a *design document* (`docs/d6_lattice_design.md`),
**not as code.** Specify:

- Data structures: D₆ point as 6-tuple of integers, projection
  map π: ℤ⁶ → ℝ³ as standard icosahedral slope, acceptance domain
  as convex polytope in internal ℝ³.
- Operations needed: membership test, nearest lattice point,
  orbit under I_h.
- Interface to existing Apeiron primitives: D₆ point → ZPhi³
  coordinate.

Costs one day, produces a reviewable artifact, means P2 can start
immediately when P1 is done rather than a week of design after the
read. Engineering follows design; design can proceed during the
wait.

### Q16c — Route 1 only when P2 is authorised. Routes 2 + 3 are wrong scope.

Routes 2 and 3 are not falsifiable within a bounded time horizon
("search for non-H₃-fundamental-region shape structure" has no
natural stopping condition). Route 1 is a finite combinatorial
object (D₆ vertex-class arrangements under H₃ fundamental-region
constraint) → "exhaustively test all arrangements" is a real
termination condition.

If Route 1 returns exhaustive NoRealisation (expected per the
pattern):
- **Don't** start Routes 2/3.
- Re-relay and reconsider Branch B4 (no-go theorem programme).
- "No 3D icosahedral Einstein exists in the substitution-tiling
  or CPS framework" becomes a real theorem if proven, and the
  computational survey across Track A + Track B n≤3 + full D₆
  CPS family is the evidence base for a proof attempt.

Routes 2 + 3 only authorised if P1 reveals specific evidence
that non-H₃ shape families or sibling lattices are promising
(turning Gate C into Branch A).

## Authorised work order

1. Write `docs/d6_lattice_design.md` per the spec above. Design
   doc only, no code. ~1 day. Reviewable artifact.
2. (Externally) pursue P1 via institutional ILL — user action.
3. After P1 reads either Branch A or remains Gate C with bounded
   conjecture: re-relay authorising P2 (Route 1 implementation)
   or re-relay authorising Branch B4 (no-go theorem programme).
