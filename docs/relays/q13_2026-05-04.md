# Q13 relay — 2026-05-04 (post-(c.3) closure, ready for (c.1))

## Context

Both (c.3) audits closed with "pipeline sound" conclusions:

- κ-vs-radius: docstring contract added; Track A's σⁿ-depth
  documented; rigorous κ_ABCK deferred but conservative bound
  κ_ABCK ≤ 2 reaches a sound verdict.
- Sibling-edge-to-edge: verified computationally; 24 face matches,
  71 τP-boundary co-tilings, 0 violations across 87 sibling pairs.

(c.1) gate per Q12c satisfied.

## Reply from Claude (web)

### Q13a — Authorise (c.1) start. No outstanding (c.3) items block.

The gate condition is satisfied. Level-2 pillar-2 tests for B, C, K
are documented, non-blocking, separate-commit material. Sibling-
face-to-face result (0 violations, 87 pairs) is cleanest possible
outcome — Goodman-Strauss citation is solid.

### Q13b — Tilings Encyclopedia ABCK check, 15-minute time-box.

Most likely place a computed κ would appear is the Encyclopedia
entry maintained by Frettlöh. Same visit, two questions:

1. Is there a 15-class list with coordinates?
2. Is κ_ABCK stated?

Record both answers in `docs/literature_notes.md` and commit with
the (c.1.1) entry. **Don't follow citation chains** as a side-
quest. 15-minute cap enforces.

### Q13c — Run with existing 9-class taxonomy. Don't wait.

The Tilings Encyclopedia check and the n=3 sweep are independent.
6 candidates whose eigenvectors don't match any 9-class triple are
already documented as `SKIPPED_NO_SHAPE_MATCH`. If the Encyclopedia
provides the missing 6 classes, those candidates get a follow-up
run — second pass, not a reason to delay the first.

The ~8 hours of compute for the 9-class sweep produces real
information regardless of what the Encyclopedia says: either some
candidates Realise (changes everything) or all return NoRealisation
(strengthens the cut-and-project pivot case).

**Concretely:** start the n=3 max_entry=3 sweep with the 9-class
taxonomy immediately after the Encyclopedia check. Run it
overnight. Document the 6 unmatched candidates explicitly in the
sweep output as `SKIPPED_TAXONOMY_GAP` with a note that they
require the 15-class extension. If the Encyclopedia provides
missing classes, schedule a follow-up sweep for those 6 as a
separate commit. Don't hold the main sweep hostage to the
taxonomy question.

## Authorised work order

1. (c.1.1) Tilings Encyclopedia ABCK check, 15-min cap,
   dual-purpose (15-class + κ_ABCK). Record in
   `literature_notes.md`.
2. (c.1.2) Lift k_max from 5 to 7 in realisation backtracker.
3. (c.1.3) n=3 PF=φ³ max_entry=3 exhaustive sweep with 9-class
   taxonomy + `SKIPPED_TAXONOMY_GAP` marker. Run overnight.
4. Document sweep result, re-relay.
