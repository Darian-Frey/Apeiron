"""Tests for apeiron.zphi — exact arithmetic over Z[phi].

Scope (per CLAUDE.md §5): the module is declared stable and must retain
full coverage. Tests should cover canonical form, addition, multiplication,
the phi^2 = phi + 1 identity, negation, equality, and hashing.
"""
